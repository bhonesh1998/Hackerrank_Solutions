#include<bits/stdc++.h>
#define pf printf
#define sf scanf
#define db double 
#define z long long int 
using namespace std ;

int main()
{
  int n,i;
    sf("%d",&n);
    int a[n];int pp=0;
    for(i=0;i<n;i++) {sf("%d",&a[i]); pp^=a[i];}
     cout << pp ; 
}
